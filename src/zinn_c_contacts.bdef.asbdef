projection;
strict ( 2 );
extensible;
use draft;

define behavior for ZINN_C_CONTACTS alias Contacts
extensible
use etag

{
field ( readonly ) contactid;
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
  use action Createdefault;
}